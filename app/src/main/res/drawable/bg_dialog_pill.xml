<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<!-- Fork: one option in a yellow-on-black dialog list, drawn as a pill rather
     than a bare line of text. Same language as the ArcaneChat-style button rows
     elsewhere in the fork: black fill, yellow hairline, fully rounded ends. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/black" />
    <stroke
        android:width="1dp"
        android:color="@color/theme_bright_yellow" />
    <corners android:radius="24dp" />
</shape>
