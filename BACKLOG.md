# Backlog — 白い熊 応用管理

Work 白い熊 has parked rather than dropped. Newest first. Remove an item when it is done,
never when it is merely stale.

_Nothing parked._

## Done

- **Sweep all the sister repos for the progress-label mismatch** (added and completed
  2026-09-09). The sweep inverted the assumption it was written on: `"text"` is the family
  convention and 応用管理 was the only app reading `"result"`. Fixed here in `+027` rather than
  in 26 sister repos — see the landmine note in `CLAUDE.md`.
